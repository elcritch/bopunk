# Automatically added by dh_pycentral
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall bopunk
	if grep -qs '^bopunk$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^bopunk$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
